POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=

PORT=
NODE_ENV=
AUTH_PROVIDER=

# Use Supabase PostgreSQL URLs in Render. Never commit real URLs.
DATABASE_URL=
DIRECT_URL=

# Generate production secrets with: openssl rand -base64 64
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES_IN=
JWT_REFRESH_EXPIRES_IN=

# Production frontend URL, for example https://app.example.com.
# Multiple allowed origins can be separated by commas.
FRONTEND_URL=

# Public frontend API URL, for example https://api.example.com/v1.
VITE_API_URL=
RATE_LIMIT_PER_MINUTE=
PRISMA_CLIENT_ENGINE_TYPE=

ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=
